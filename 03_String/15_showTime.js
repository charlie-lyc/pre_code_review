// 시간, 분, 초를 입력받아 현재 시각을 나타내는 메세지를 리턴해야 합니다.

function showTime(hour, min, sec) {
  return `현재 시각은 ${hour}시 ${min}분 ${sec}초 입니다.`;
}

/** Template Strings (=Template literals) 
 * ` ` :  grave accent, back quote, back tick
 * ${} : place holder
 **/
